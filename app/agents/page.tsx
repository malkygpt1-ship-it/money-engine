import { agents } from "@/lib/demo-data";
import { Status } from "@/components/status";

export default function AgentsPage(){return <><div className="topbar"><div><div className="eyebrow">Agent workforce</div><h1>Agents</h1><div className="sub">Autonomy is bounded by action risk, cost and reversibility.</div></div><div className="badge">3 green · 2 amber · 1 red</div></div><div className="grid two-col"><section className="card"><div className="section-title"><h2>Workers</h2><span>live registry</span></div><div className="agent-list">{agents.map(a=><div className="agent" key={a.id} style={{padding:"15px 0"}}><div><div className="agent-name" style={{fontSize:15}}>{a.name}</div><div className="agent-role">{a.role}</div><div className="meta" style={{marginTop:7}}>{a.output}</div></div><div style={{textAlign:"right"}}><Status value={a.status}/><div className={`autonomy ${a.autonomy}`} style={{marginTop:8}}>{a.autonomy} autonomy</div><div className="meta">{a.lastRun}</div></div></div>)}</div></section><section className="card"><div className="section-title"><h2>Autonomy policy</h2><span>default controls</span></div><div className="needs"><div className="need"><b style={{color:"var(--accent)"}}>GREEN</b><br/><span className="meta">Research, scoring, analytics, drafting and internal database writes. Runs without approval.</span></div><div className="need"><b>AMBER</b><br/><span className="meta">Asset generation and publishing inside explicit spend and volume limits.</span></div><div className="need"><b style={{color:"var(--danger)"}}>RED</b><br/><span className="meta">Production code changes, refunds, destructive actions and meaningful spend require approval.</span></div></div><div className="code" style={{marginTop:16}}>{`policy:\n  green_daily_budget_gbp: 5\n  amber_job_budget_gbp: 2\n  max_publications_per_day: 5\n  require_human_for:\n    - production_code\n    - refunds\n    - destructive_actions\n    - price_change_over_25_percent`}</div></section></div></>}
